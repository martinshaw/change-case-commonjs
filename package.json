{
  "name": "change-case-commonjs-root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build-all": "nx run-many --target=build --all",
    "prepare": "npx ts-scripts install"
  },
  "devDependencies": {
    "@borderless/ts-scripts": "^0.15.0",
    "nx": "18.0.0",
    "typescript": "^5.2.2"
  },
  "nx": {
    "targets": {}
  }
}
