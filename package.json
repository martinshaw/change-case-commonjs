{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "ts-scripts install"
  },
  "devDependencies": {
    "@borderless/ts-scripts": "^0.15.0",
    "typescript": "^5.2.2"
  }
}
